<!-- ACTIVITY CARD -->
<view data-id='{{activity.id}}' bindtap='goToShow' class="card">
  <view class="img-and-price">
    <image class="image" mode="aspectFill" src="{{activity.main_photo}}" 
      placeholder="Child Development Activity" 
      style="flex-shrink: 0;"></image>
  </view>

  <view class="card-info">
    <view class="activity-title-provider">
      <view class="activity-title>">{{activity.title}}</view>
      <view class="provider-name"> by {{activity.organizer.name}}</view>
    </view>

    <view class="container-heart-date">
      <view class="heart-div">
        <view class="body">{{activity.num_of_favs}}</view>
        <image class="activity-heart" mode="aspectFit" src='../../pages/images/fullheart.png'></image>
      </view> 
        <view class="date">{{activity.next_session.day_of_week}}, {{activity.next_session.date}}</view>
    </view>

    </view>
</view>



