<!--pages/profile/profile.wxml-->

<!-- Tree/Image Avatar -->
<view class="img-container">
  <image src="https://us.123rf.com/450wm/kudryashka/kudryashka1609/kudryashka160900327/64881825-stock-vector-colorful-art-tree-for-your-design-vector-illustration.jpg?ver=6" class="img-background"></image>
  <image class="avatar-large" alt="avatar-large" src="{{userInfo.avatarUrl}}" />
</view>

<!-- Bookings Bar -->
<!-- <view class="line-container">
    <view class="my-bookings">My Bookings</view>
    <view class="my-favorites">My Favorites</view>
</view> -->

<!-- IF user is not logged in, displaying the login button -->
<button class='login-button' wx:if="{{!hasUserInfo}}" open-type="getUserInfo" bindgetuserinfo="getUserInfo">Please log in</button>

<view wx:if="{{hasUserInfo}}">
  <view class="welcome">Welcome {{userInfo.nickName}}</view>
</view>




<!-- BOOKING BAR IRENE -->
<view wx:if="{{hasUserInfo}}">
<view class="tabs-container">
  <view class="tab-titles">
    <view class="tab-title {{activeTab == 0 ? 'active': ''}}" bindtap="switchTab" data-tab="0">My Bookings</view>
    <view class="tab-title {{activeTab == 1 ? 'active': ''}}" bindtap="switchTab" data-tab="1">My Favorites</view>
  </view>

  <view class="tab-page" wx:if="{{activeTab == 1}}">
    
    <!-- <view wx:key='index' wx:for="{{activities}}" wx:for-item="activity" >
     <card activity="{{activity}}" />
    </view> -->

  </view>

  <view class="tab-page" wx:if="{{activeTab == 0}}">
    <view wx:for="{{bookings}}" wx:for-item="session" >
    <sessions-card activity="{{session.sesssion}}" />
  </view>

  </view>
</view>

<!-- User Bookings -->

<!-- <view class="card-container">
  <view class="profile-card">
    <view class="title-date">
      <view class="title">Activity Title</view>
      <view class="date">2020-12-25</view>
    </view>
      <button class="btn-book" bindtap="book"> Book </button> -->
      <!-- <button class="profile-button">Paid</button> -->
  <!-- </view> -->
<!-- </view> -->

<!-- <button open-type="getUserInfo" bindgetuserinfo="getUserInfo">Login</button> -->

